[package]
name = "streamline"
edition = "2024.beta"
version = "0.1.0"
# Upgraded 2026-07-19: create_stream_v3, treasury reserve (to_reserve/
# from_reserve), then stream::start_payroll (payer-side LOCKED→DRIPPING start).
# `published-at` = latest version (upgrade from); the [addresses] streamline =
# ORIGINAL package (type origin, unchanged across upgrades).
published-at = "0x2362f832f056ae74e4b093a3ae6947f91970f2434176fd7d373d004ea1f08020"

# The Sui / MoveStdlib framework dependencies are managed automatically by the
# Sui CLI, pinned to the active environment's framework version.

[addresses]
streamline = "0x597f34fee3b246cbabdb8b8133c4b5e7aa5a15899e74df40acf61080b776a794"
