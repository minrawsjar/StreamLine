"use client";

/**
 * Legacy entry — desktop now uses /app/pro routes + ProShell.
 * Kept as a thin alias for any remaining imports.
 */
export { OverviewScreen as ProDashboard } from "./screens/OverviewScreen";
