/** Full-screen overlay for in-shell modals (create, transfer). */
export const phoneFlowOverlay =
  "absolute inset-0 z-30 flex flex-col bg-white/92 backdrop-blur-xl";

/** Sticky footer for phone flows. */
export const phoneFlowFooter =
  "shrink-0 flex flex-col gap-2 border-t border-black/8 bg-white/70 backdrop-blur-md px-4 py-3";

/** Glass card on the phone wallpaper. */
export const phoneGlassCard =
  "rounded-2xl border border-white/70 bg-white/88 shadow-[0_8px_24px_rgba(0,0,0,0.08)] backdrop-blur-md";
